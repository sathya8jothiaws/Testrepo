---
- 
 hosts: all
 gather_facts: true
           
 tasks:
   - name: Example from an Ansible Playbook
     win_ping:
     register : results
     
   - name: Display all cluster names
     ansible.builtin.debug:
       msg: "{{ results | json_query('ping') }}"" \n "{{ results|json_query('invocation.module_args.data') }}"
